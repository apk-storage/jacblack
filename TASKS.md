# Задачи JacBlack

Рабочий список при коде. Крупные вехи — в `personal-hub/jacred/GOALS.md`
(их видно на дашборде), durable-выводы и грабли — в файлах памяти.

Обозначения: `[ ]` не начата · `[~]` в работе · `[x]` готово И ПРОВЕРЕНО.
У готовой задачи всегда приписано, **чем** проверял. У задачи `[~]` —
«сделано / осталось», чтобы следующая сессия продолжила без объяснений.

---

## В работе

- [~] **Задача #15 «В Лампе»** — кнопка отправляет раздачу на устройство
      через сокет CUB.
      **Сделано:** клиентская часть написана и выкачена (`webui/src/lib/cub/socket.ts`),
      диалог показывает техданные сокета, CSP пропускает `ws`/`wss`, зеркало
      не переключается на каждом открытии, список устройств фильтруется как
      в Лампе. Проба сокета с ноды показала, что CUB отвечает даже на пустой
      аккаунт — значит сервер вещает, а разбирается клиент.
      **Осталось:** живой тест на телевизоре — включить Терминал в Лампе,
      задать код, войти в свою учётку cub.rip и проверить приём команды.
      Делаем вместе с Сергеем, без телевизора не проверить.

## Ближайшее

- [ ] **Статистика запросов в разрезах** (страна, устройство, веб/Лампа,
      прямой/зеркало). План из трёх шагов расписан в `GOALS.md`. Первый шаг
      дешёвый и важен тем, что данные пойдут только вперёд: зеркало должно
      слать `X-Mirror`, а формат лога — писать `$http_cf_ipcountry`.
- [ ] **Остаток раздач без года** — 124 130 записей без года и без кода.
      В основном не-видео у nnmclub и старые релизы. В карточки не попадают
      и не мешают, отдельного пути пока не требуют.
- [ ] **Снимки страниц устаревают.** Сторож ловит регресс в коде, а смену
      вёрстки на сайте — нет; её видно только по `lastnewtor`. Пересобирать
      снимки раз в несколько месяцев.
- [ ] **Чтение кода:** внутренности `Tracks` (~2900 строк) и фронта.

## Сделано 20.08.2026

- [x] **Чужие сборки Лампы отсечены по Origin.** Свои — `lampa.mx`,
      `yumata.github.io`, `lampa.byskaz.ru`, наши домены, `localhost` и
      `Origin: null`; остальным явным Origin — 403. Запросы БЕЗ Origin
      проходят: их шлёт подавляющее большинство настоящих клиентов
      (телевизоры, okhttp, Android TV). `null` тоже пропускаем — его шлёт
      webview с локальной страницы, в логе это Philips на TitanOS и iPad.
      Правило записано картой в `nginx.conf`, проверка — в обоих `location`
      vhost `jac.black`; новая чужая сборка отсечётся сама, без правки списка.
      *Проверял:* живыми запросами через Cloudflare — без Origin, `lampa.mx`,
      `yumata.github.io`, `byskaz`, `null` дают 200 и **457 раздач** по
      «matrix»; `bylampa.online`, `l.de.anuok.ru`, `lampa1.ru` — 403. Зеркало
      `ru.jac.black` ведёт себя так же. За первые полчаса отсечены ровно
      чужие, своих ноль. Бэкапы `/root/nginx.conf.before-origin-*`.

- [x] **Статистика обращений считает то, что называет.** Три ошибки подряд,
      и все — в мерке, а не в данных:
      1. Разбирался только текущий лог — «сутки» на деле были 8 часами
         (4 123 строки против 243 603 за 15 дней). Теперь читаются и
         ротированные, включая `.gz`.
      2. Половину строк (121 996) давали проверки живости: чужой Gatus с
         Hetzner берёт `/health` каждые 15 секунд, наш Kuma — корень раз в
         30 секунд. Отделены, в сводке живой трафик считается без них.
      3. Поиском считалась только ручка Torznab. Чужой парсер из немецкого
         датацентра ходит родной `/api/v1.0/torrents` — и выглядел безобидным
         с «поисков 0» при 531 запросе. На деле все 531 — поиски.
      *Проверял:* пересчётом начисто; после правки живой трафик — 121 607
      запросов и 92 383 поиска от ~12 000 адресов, картина сходится с
      прежним замером аудитории (98% нагрузки из Лампы).
      *Скрипт:* `/usr/local/bin/jacblack-traffic-stats` на awg, крон ежечасно,
      база `Data/traffic.db`.

## Сделано 19.08.2026

- [x] **Импорт архива nyaa из дампа AnimeTosho — завершён.**
      Загружены все **863 940** записей; nyaa стала крупнейшим источником базы —
      **870 635 раздач** против 8 073 до импорта. Живость опрашивалась на входе,
      «без ответа трекеров» за весь прогон — ноль.
      *Проверял:* «Wistoria S2» — 295 раздач, из них nyaa 276 (было 4);
      «Игра престолов» — 269, «Дюна» — 122, ответ 1.9–3.3 с. Задание импорта
      снято из крона, сторож уведомления снял себя сам.
      *Грабли:* трекеры опрашивать строго по очереди (залпом в семь раз
      медленнее); курсор `Data/temp/nyaa_import_cursor.txt` переживает
      перезапуск, выгрузка — `Data/nyaa-archive.jsonl`.

- [x] **Серии аниме и диапазоны разбираются.** Разбор знал только «S01E05» и
      «1x05», а у аниме номер серии пишут через тире — вся аниме-выдача уходила
      без серии. Диапазоны «[01-12]», «S01E01-E12», «(1-6 из 6)» теперь дают
      сезон без серии: это набор, и номер одной серии там был бы враньём.
      *Проверял:* Torznab-путём — «Wistoria S2 - 01…04» отдаёт season=2 и
      ep=1…4; 818 тестов, из них 13 новых.

- [x] **Дорожки у зарубежных раздач и ложные озвучки.** У piratebay и yts
      показывалось только разрешение: русской дорожки там нет, и поле пустовало.
      Теперь разбираются «Dual Audio», «MULTI», «2xUkr/Eng», «Sub Eng», языки
      eng/jpn. Попутно убраны ЛОЖНЫЕ озвучки: список из ~900 студий искался
      подстрокой и находил «Inter» в «Winterfell», «Anika» в «Itsunomanika»,
      «Rumble» в «School Rumble» — человек видел озвучку, которой нет.
      Список применяется только там, где русский перевод возможен.
      *Проверял:* 805 тестов, живая выдача, запущен пересчёт метаданных по базе.

- [x] **Склейки перестали считаться отдельными источниками.** Код был написан
      16.08 (`Infrastructure/Parsing/TrackerNames`, правка `StatsCollector`),
      но **три дня не доезжал до сервера**: сборочный каталог на awg живёт
      отдельно от репозитория, а я копировал туда только те файлы, которые
      правил в тот момент. `TrackerNames.cs` там просто не появлялся, и сборки
      шли без него.
      **Как чинить впредь:** переносить весь код `git archive HEAD` и сверять
      каталог с репозиторием (`diff -rq`), а не копировать файлы поштучно.
      «Собрано» и «выкачено» — разные утверждения, проверять надо второе.
      **Проверено:** после выката `/stats/trackers` отдаёт **19 источников
      вместо 234, склеек ноль**; тестов 777 (приехали и тесты статистики,
      которых на сервере тоже не было); сверка git с каталогом расхождений
      по коду не показала.

## Сделано 18.08.2026

- [x] **Аниме не находилось из карточки с годом.** По «Tsue to Tsurugi no
      Wistoria S2» с `year=2024` выдача была пуста, без года — четыре раздачи.
      Причина: правило «человек указал год, а у раздачи год не разобран —
      выбрасываем» (ставилось против сериалов-тёзок в карточке фильма
      «Одиссея» 2026). У аниме года в названии не бывает вовсе —
      `[SubsPlease] ... S2 - 04`, — и под нож попадала вся аниме-выдача.
      Послабление: раздача без года проходит, если карточка объявила себя
      сериалом/аниме И в названии есть номер сезона или серии.
      **Проверено:** запросом Сергея — 0 → 4 раздачи; `Sakamoto Days` 0 → 57,
      `Dandadan` 0 → 44; карточка фильма «Одиссея» 2026 отдаёт 12 раздач, все
      2026 года (старая беда не вернулась); 768 тестов зелёные, из них 4 новых
      в `tests/JacBlack.Tests/Search/AnimeWithoutYearTests.cs`.
      Замечено попутно: у `FilterByCardTitle` есть «последний рубеж» — при
      пустом строгом отборе он отдаёт совпавшие по названию записи без года.
      Тесты на это правило обязаны иметь якорную раздачу с годом, иначе
      проверяют рубеж, а не правило.

## Сделано 15.08.2026

- [x] **Пересборка из актуального git.** Сборочный каталог на awg разошёлся
      с репозиторием: исходники были от 31 июля, без `webui/src/lib/cub/socket.ts`,
      и `.csproj` ещё со старым именем `JacRed`. Перенос через `git archive`.
      *Проверял:* 730 тестов, контейнер здоров, сайт 200 из Германии и России,
      обходы продолжились с сохранённого места.
- [x] **Удалённые раздачи у kinozal, nnmclub, toloka** — оказалось сделано
      раньше. Убран метод-сирота `DropDeadReleases`, знавший только два
      трекера из пяти.
      *Проверял:* признаки живьём (у обоих несуществующая тема даёт 404);
      по запросам «Матрица», «Дюна», «Аватар» из 11 660 мёртвых kinozal не
      просочилась ни одна.
- [x] **Доля проверенных на странице статистики.** Плюс суточный пересчёт
      сводки — без него на сайте висела бы цифра от 30 июля.
      *Проверял:* пересчёт дал 87.8% против прежних 40.7%, ручка отдаёт блок
      наружу, текст виден в обоих собранных экранах.
- [x] **21 пропущенный раздел rutracker.** Раздача не находилась, потому что
      лежала в подразделе, а в списке был только родитель.
      *Проверял:* обход прошёл новые разделы, раздача появилась (23 сида,
      73.21 ГБ), видна и в вебе, и по пути Лампы.
- [x] **Lostfilm переведён на разбор деревом** — последний парсер на
      регулярках по разметке.
      *Проверял:* 737 тестов, эталонные снимки совпали поле в поле, живой
      обход после выката дал те же 15 записей, что и до.
- [x] **Год восстанавливается по коду IMDB** у раздач, где его нет в
      названии (сценовые релизы).
      *Проверял:* 28 201 запись получила год; по карточке «Пацаны» стало
      271 раздача, из них 68 piratebay, которые раньше отсеивались.
- [x] **Копии одного файла свёрнуты в одну строку** в веб-выдаче.
      *Проверял:* 289 строк → 149 при тех же 149 уникальных файлах (ни один
      не потерян), путь Лампы не задет, 744 теста.
- [x] **Не-видео отсеивается и вычищено** — книги, музыка, картинки, софт
      приходили с типом `movie`.
      *Проверял:* удалено 32 447 записей, после чистки «Дюна» 363,
      «Матрица» 427, «Дом дракона» 149, сайт и зеркало отвечают, 764 теста.

